export default function Base64ToolPlaceholder() {
  return (
    <div className="flex h-full min-h-0 flex-col items-center justify-center bg-zinc-950 px-4 text-center">
      <h1 className="text-sm font-medium text-zinc-300">Base64</h1>
      <p className="mt-2 max-w-xs text-xs leading-relaxed text-zinc-400">
        Encode and decode — placeholder. This screen will host the Base64 tool.
      </p>
    </div>
  );
}
